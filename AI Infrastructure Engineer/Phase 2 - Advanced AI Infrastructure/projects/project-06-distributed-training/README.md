# Project 06: Distributed LLM Training Pipeline

An end-to-end hands-on project to train a transformer-based language model across a distributed GPU cluster.

## Architecture
- Orchestrated using SLURM / Ray cluster manager.
- Implemented via PyTorch Fully Sharded Data Parallel (FSDP).
- Monitored with TensorBoard and WandB.
